# 4. creating the bar graph
ggplot(d, aes(x = species, y = species_mean_BMI, fill = species_mean_BMI > 30)) +
geom_bar(x = species, y = species_mean_BMI)
#### 0. Setup Workspace ####
graphics.off()
rm(list=ls())
library(tidyverse)
# 1. loading data and filtering species
# only loading relevant columns
d <- starwars |>
select(name, mass, height, species) |>
filter(str_detect(species, "^(G|D|W)") | str_detect(species, "man$"))
# 2. character BMI, species mean BMI, removing na values
# drop_na() will remove all na rows from the dataframe
d <- d |>
drop_na() |>
mutate(character_BMI = mass / (height/100)^2) |>
group_by(species) |>
mutate(species_mean_BMI = mean(character_BMI))
# 3. adding a column named is_bigger for mean BMI > 30
d <- d |>
mutate(is_bigger = if_else(species_mean_BMI>30, "yes", "no"))
# 4. creating the bar graph
ggplot(d, aes(x = species, y = species_mean_BMI, fill = species_mean_BMI > 30)) +
geom_bar(x = species, y = species_mean_BMI)
# 4. creating the bar graph
ggplot(d, aes(x = species, y = species_mean_BMI, fill = species_mean_BMI > 30)) +
geom_bar(x = species)
# 4. creating the bar graph
ggplot(d, aes(x = species, y = species_mean_BMI, fill = species_mean_BMI > 30)) +
geom_bar(x)
# 4. creating the bar graph
ggplot(d, aes(x = species, y = species_mean_BMI, fill = species_mean_BMI > 30)) +
geom_bar(species)
#### 0. Setup Workspace ####
graphics.off()
rm(list=ls())
library(tidyverse)
# 1. loading data and filtering species
# only loading relevant columns
d <- starwars |>
select(name, mass, height, species) |>
filter(str_detect(species, "^(G|D|W)") | str_detect(species, "man$"))
# 2. character BMI, species mean BMI, removing na values
# drop_na() will remove all na rows from the dataframe
d <- d |>
drop_na() |>
mutate(character_BMI = mass / (height/100)^2) |>
group_by(species) |>
mutate(species_mean_BMI = mean(character_BMI))
# 3. adding a column named is_bigger for mean BMI > 30
d <- d |>
mutate(is_bigger = if_else(species_mean_BMI>30, "yes", "no"))
# 4. creating the bar graph
ggplot(d, aes(x = species, y = species_mean_BMI, fill = species_mean_BMI > 30)) +
geom_bar(species)
# 4. creating the bar graph
ggplot(d, aes(x = species, y = species_mean_BMI) +
# 4. creating the bar graph
ggplot(d, aes(x = species, y = species_mean_BMI)) +
geom_bar(species)
#### 0. Setup Workspace ####
graphics.off()
rm(list=ls())
library(tidyverse)
# 1. loading data and filtering species
# only loading relevant columns
d <- starwars |>
select(name, mass, height, species) |>
filter(str_detect(species, "^(G|D|W)") | str_detect(species, "man$"))
# 2. character BMI, species mean BMI, removing na values
# drop_na() will remove all na rows from the dataframe
d <- d |>
drop_na() |>
mutate(character_BMI = mass / (height/100)^2) |>
group_by(species) |>
mutate(species_mean_BMI = mean(character_BMI))
# 3. adding a column named is_bigger for mean BMI > 30
d <- d |>
mutate(is_bigger = if_else(species_mean_BMI>30, "yes", "no"))
# 4. creating the bar graph
ggplot(d, aes(x = species, y = species_mean_BMI)) +
geom_bar(species)
stat = "identity"
#### 0. Setup Workspace ####
graphics.off()
rm(list=ls())
library(tidyverse)
# 1. loading data and filtering species
# only loading relevant columns
d <- starwars |>
select(name, mass, height, species) |>
filter(str_detect(species, "^(G|D|W)") | str_detect(species, "man$"))
# 2. character BMI, species mean BMI, removing na values
# drop_na() will remove all na rows from the dataframe
d <- d |>
drop_na() |>
mutate(character_BMI = mass / (height/100)^2) |>
group_by(species) |>
mutate(species_mean_BMI = mean(character_BMI))
# 3. adding a column named is_bigger for mean BMI > 30
d <- d |>
mutate(is_bigger = if_else(species_mean_BMI>30, "yes", "no"))
# 4. creating the bar graph
ggplot(d, aes(x = species, y = species_mean_BMI, fill = species_mean_BMI > 30)) +
geom_bar(stat = "identity")
#### 0. Setup Workspace ####
graphics.off()
rm(list=ls())
library(tidyverse)
# 1. loading data and filtering species
# only loading relevant columns
d <- starwars |>
select(name, mass, height, species) |>
filter(str_detect(species, "^(G|D|W)") | str_detect(species, "man$"))
# 2. character BMI, species mean BMI, removing na values
# drop_na() will remove all na rows from the dataframe
d <- d |>
drop_na() |>
mutate(character_BMI = mass / (height/100)^2) |>
group_by(species) |>
mutate(species_mean_BMI = mean(character_BMI))
# 3. adding a column named is_bigger for mean BMI > 30
d <- d |>
mutate(is_bigger = if_else(species_mean_BMI>30, "yes", "no"))
# Part 2 - Visualization
# 4. creating the bar graph
geom_bar(stat = "identity")
I, fill = species_mean_BMI > 30)
#### 0. Setup Workspace ####
graphics.off()
rm(list=ls())
library(tidyverse)
# 1. loading data and filtering species
# only loading relevant columns
d <- starwars |>
select(name, mass, height, species) |>
filter(str_detect(species, "^(G|D|W)") | str_detect(species, "man$"))
# 2. character BMI, species mean BMI, removing na values
# drop_na() will remove all na rows from the dataframe
d <- d |>
drop_na() |>
mutate(character_BMI = mass / (height/100)^2) |>
group_by(species) |>
mutate(species_mean_BMI = mean(character_BMI))
# 3. adding a column named is_bigger for mean BMI > 30
d <- d |>
mutate(is_bigger = if_else(species_mean_BMI>30, "yes", "no"))
# 4. creating the bar graph
ggplot(d, aes(x = species, y = species_mean_BMI, fill = species_mean_BMI > 30)) +
geom_bar(stat = "identity")
# 4. creating the bar graph
ggplot(d, aes(x = species, y = species_mean_BMI)) +
geom_bar(stat = "identity")
#### 0. Setup Workspace ####
graphics.off()
rm(list=ls())
library(tidyverse)
# 1. loading data and filtering species
# only loading relevant columns
d <- starwars |>
select(name, mass, height, species) |>
filter(str_detect(species, "^(G|D|W)") | str_detect(species, "man$"))
# 2. character BMI, species mean BMI, removing na values
# drop_na() will remove all na rows from the dataframe
d <- d |>
drop_na() |>
mutate(character_BMI = mass / (height/100)^2) |>
group_by(species) |>
mutate(species_mean_BMI = mean(character_BMI))
# 3. adding a column named is_bigger for mean BMI > 30
d <- d |>
mutate(is_bigger = if_else(species_mean_BMI>30, "yes", "no"))
# 4. creating the bar graph
ggplot(d, aes(x = species, y = species_mean_BMI)) +
geom_bar(stat = "identity")
# 4. creating the bar graph
ggplot(d, aes(x = species, y = species_mean_BMI)) +
geom_bar(x = species)
# 4. creating the bar graph
ggplot(d, aes(x = species, y = species_mean_BMI)) +
geom_bar(x = species, stat = "identity")
#### 0. Setup Workspace ####
graphics.off()
rm(list=ls())
library(tidyverse)
# 1. loading data and filtering species
# only loading relevant columns
d <- starwars |>
select(name, mass, height, species) |>
filter(str_detect(species, "^(G|D|W)") | str_detect(species, "man$"))
# 2. character BMI, species mean BMI, removing na values
# drop_na() will remove all na rows from the dataframe
d <- d |>
drop_na() |>
mutate(character_BMI = mass / (height/100)^2) |>
group_by(species) |>
mutate(species_mean_BMI = mean(character_BMI))
# 3. adding a column named is_bigger for mean BMI > 30
d <- d |>
mutate(is_bigger = if_else(species_mean_BMI>30, "yes", "no"))
# 4. creating the bar graph
ggplot(d, aes(x = species, y = species_mean_BMI)) +
geom_bar(x = species, stat = "identity")
# 4. creating the bar graph
ggplot(d, aes(x = species, y = species_mean_BMI)) +
geom_bar(aes(color = drv))
# 4. creating the bar graph
ggplot(d, aes(x = species, y = species_mean_BMI)) +
geom_bar(aes(fill = drv))
# 4. creating the bar graph
ggplot(d, aes(x = species, y = species_mean_BMI, fill = drv)) +
geom_bar()
#### 0. Setup Workspace ####
graphics.off()
rm(list=ls())
library(tidyverse)
# 1. loading data and filtering species
# only loading relevant columns
d <- starwars |>
select(name, mass, height, species) |>
filter(str_detect(species, "^(G|D|W)") | str_detect(species, "man$"))
# 2. character BMI, species mean BMI, removing na values
# drop_na() will remove all na rows from the dataframe
d <- d |>
drop_na() |>
mutate(character_BMI = mass / (height/100)^2) |>
group_by(species) |>
mutate(species_mean_BMI = mean(character_BMI))
# 3. adding a column named is_bigger for mean BMI > 30
d <- d |>
mutate(is_bigger = if_else(species_mean_BMI>30, "yes", "no"))
# 4. creating the bar graph
ggplot(d, aes(x = species, y = species_mean_BMI, fill = drv)) +
geom_bar()
# 4. creating the bar graph
ggplot(d, aes(x = species, y = species_mean_BMI, fill = is_bigger)) +
geom_bar()
# 4. creating the bar graph
ggplot(d, aes(x = species, y = species_mean_BMI, fill = is_bigger)) +
geom_bar(stat = "identity")
d
#### 0. Setup Workspace ####
graphics.off()
rm(list=ls())
library(tidyverse)
# 1. loading data and filtering species
# only loading relevant columns
d <- starwars |>
select(name, mass, height, species) |>
filter(str_detect(species, "^(G|D|W)") | str_detect(species, "man$"))
# 2. character BMI, species mean BMI, removing na values
# drop_na() will remove all na rows from the dataframe
d <- d |>
drop_na() |>
mutate(character_BMI = mass / (height/100)^2) |>
group_by(species) |>
mutate(species_mean_BMI = mean(character_BMI))
# 3. adding a column named is_bigger for mean BMI > 30
d <- d |>
mutate(is_bigger = if_else(species_mean_BMI>30, "yes", "no"))
# 4. creating the bar graph
ggplot(d, aes(x = species, y = species_mean_BMI, fill = is_bigger)) +
geom_bar(stat = "identity")
d
# 4. creating the bar graph
g <- d |> group_by(species) |>
ggplot(aes(x = species, y = species_mean_BMI, fill = is_bigger)) +
geom_bar(stat = "identity")
# 4. creating the bar graph
s <- d |> group_by(species)
#### 0. Setup Workspace ####
graphics.off()
rm(list=ls())
library(tidyverse)
# 1. loading data and filtering species
# only loading relevant columns
d <- starwars |>
select(name, mass, height, species) |>
filter(str_detect(species, "^(G|D|W)") | str_detect(species, "man$"))
# 2. character BMI, species mean BMI, removing na values
# drop_na() will remove all na rows from the dataframe
d <- d |>
drop_na() |>
mutate(character_BMI = mass / (height/100)^2) |>
group_by(species) |>
mutate(species_mean_BMI = mean(character_BMI))
# 3. adding a column named is_bigger for mean BMI > 30
d <- d |>
mutate(is_bigger = if_else(species_mean_BMI>30, "yes", "no"))
# 4. creating the bar graph
s <- d |> group_by(species)
s
# 4. creating the bar graph
s <- d |>
select(species, species_mean_BMI, is_bigger) |>
group_by(species)
#### 0. Setup Workspace ####
graphics.off()
rm(list=ls())
library(tidyverse)
# 1. loading data and filtering species
# only loading relevant columns
d <- starwars |>
select(name, mass, height, species) |>
filter(str_detect(species, "^(G|D|W)") | str_detect(species, "man$"))
# 2. character BMI, species mean BMI, removing na values
# drop_na() will remove all na rows from the dataframe
d <- d |>
drop_na() |>
mutate(character_BMI = mass / (height/100)^2) |>
group_by(species) |>
mutate(species_mean_BMI = mean(character_BMI))
# 3. adding a column named is_bigger for mean BMI > 30
d <- d |>
mutate(is_bigger = if_else(species_mean_BMI>30, "yes", "no"))
# 4. creating the bar graph
s <- d |>
select(species, species_mean_BMI, is_bigger) |>
group_by(species)
s
view(s)
s
# 4. creating the bar graph
s <- d |>
select(species, species_mean_BMI, is_bigger) |>
distinct()
view(s)
# 4. creating the bar graph
g <- d |>
select(species, species_mean_BMI, is_bigger) |>
distinct() |>
ggplot(aes(x = species, y = species_mean_BMI, fill = is_bigger)) +
geom_bar(stat = "identity") +
labs(title = "The biggest species in Star Wars", x = "species",
y = "Body mass index (kg/m^2)")
#### 0. Setup Workspace ####
graphics.off()
rm(list=ls())
library(tidyverse)
# 1. loading data and filtering species
# only loading relevant columns
d <- starwars |>
select(name, mass, height, species) |>
filter(str_detect(species, "^(G|D|W)") | str_detect(species, "man$"))
# 2. character BMI, species mean BMI, removing na values
# drop_na() will remove all na rows from the dataframe
d <- d |>
drop_na() |>
mutate(character_BMI = mass / (height/100)^2) |>
group_by(species) |>
mutate(species_mean_BMI = mean(character_BMI))
# 3. adding a column named is_bigger for mean BMI > 30
d <- d |>
mutate(is_bigger = if_else(species_mean_BMI>30, "yes", "no"))
# 4. creating the bar graph
g <- d |>
select(species, species_mean_BMI, is_bigger) |>
distinct() |>
ggplot(aes(x = species, y = species_mean_BMI, fill = is_bigger)) +
geom_bar(stat = "identity") +
labs(title = "The biggest species in Star Wars", x = "species",
y = "Body mass index (kg/m^2)")
ggplot(species_summary, aes(x = species, y = species_mean_BMI, fill = is_bigger))
#### 0. Setup Workspace ####
graphics.off()
rm(list=ls())
library(tidyverse)
# 1. loading data and filtering species
# only loading relevant columns
d <- starwars |>
select(name, mass, height, species) |>
filter(str_detect(species, "^(G|D|W)") | str_detect(species, "man$"))
# 2. character BMI, species mean BMI, removing na values
# drop_na() will remove all na rows from the dataframe
d <- d |>
drop_na() |>
mutate(character_BMI = mass / (height/100)^2) |>
group_by(species) |>
mutate(species_mean_BMI = mean(character_BMI))
# 3. adding a column named is_bigger for mean BMI > 30
d <- d |>
mutate(is_bigger = if_else(species_mean_BMI>30, "yes", "no"))
# 4. creating the bar graph
species_summary <- d |>
select(species, species_mean_BMI, is_bigger) |>
distinct()
ggplot(species_summary, aes(x = species, y = species_mean_BMI, fill = is_bigger))
geom_bar(stat = "identity") +
labs(title = "The biggest species in Star Wars", x = "species",
y = "Body mass index (kg/m^2)")
ggplot(species_summary, aes(x = species, y = species_mean_BMI, fill = is_bigger)) +
geom_bar(stat = "identity") +
labs(title = "The biggest species in Star Wars", x = "species",
y = "Body mass index (kg/m^2)")
#### 0. Setup Workspace ####
graphics.off()
rm(list=ls())
library(tidyverse)
# 1. loading data and filtering species
# only loading relevant columns
d <- starwars |>
select(name, mass, height, species) |>
filter(str_detect(species, "^(G|D|W)") | str_detect(species, "man$"))
# 2. character BMI, species mean BMI, removing na values
# drop_na() will remove all na rows from the dataframe
d <- d |>
drop_na() |>
mutate(character_BMI = mass / (height/100)^2) |>
group_by(species) |>
mutate(species_mean_BMI = mean(character_BMI))
# 3. adding a column named is_bigger for mean BMI > 30
d <- d |>
mutate(is_bigger = if_else(species_mean_BMI>30, "yes", "no"))
# 4. creating the bar graph
species_summary <- d |>
select(species, species_mean_BMI, is_bigger) |>
distinct()
ggplot(species_summary, aes(x = species, y = species_mean_BMI, fill = is_bigger)) +
geom_bar(stat = "identity") +
labs(title = "The biggest species in Star Wars", x = "species",
y = "Body mass index (kg/m^2)", color = "BMI > 30")
ggplot(species_summary, aes(x = species, y = species_mean_BMI, fill = is_bigger)) +
geom_bar(stat = "identity") +
labs(title = "The biggest species in Star Wars", x = "species",
y = "Body mass index (kg/m^2)", color = "BMI > 30")
ggplot(species_summary, aes(x = species, y = species_mean_BMI, fill = is_bigger)) +
geom_bar(stat = "identity") +
labs(title = "The biggest species in Star Wars", x = "species",
y = "Body mass index (kg/m^2)", fill = "BMI > 30")
#### 0. Setup Workspace ####
graphics.off()
rm(list=ls())
library(tidyverse)
# 1. loading data and filtering species
# only loading relevant columns
d <- starwars |>
select(name, mass, height, species) |>
filter(str_detect(species, "^(G|D|W)") | str_detect(species, "man$"))
# 2. character BMI, species mean BMI, removing na values
# drop_na() will remove all na rows from the dataframe
d <- d |>
drop_na() |>
mutate(character_BMI = mass / (height/100)^2) |>
group_by(species) |>
mutate(species_mean_BMI = mean(character_BMI))
# 3. adding a column named is_bigger for mean BMI > 30
d <- d |>
mutate(is_bigger = if_else(species_mean_BMI>30, "yes", "no"))
# 4. creating the bar graph
species_summary <- d |>
select(species, species_mean_BMI, is_bigger) |>
distinct()
ggplot(species_summary, aes(x = species, y = species_mean_BMI, fill = is_bigger)) +
geom_bar(stat = "identity") +
labs(title = "The biggest species in Star Wars", x = "species",
y = "Body mass index (kg/m^2)", fill = "BMI > 30")
#### 0. Setup Workspace ####
graphics.off()
rm(list=ls())
library(tidyverse)
# 1. loading data and filtering species
# only loading relevant columns
d <- starwars |>
select(name, mass, height, species) |>
filter(str_detect(species, "^(G|D|W)") | str_detect(species, "man$"))
# 2. character BMI, species mean BMI, removing na values
# drop_na() will remove all na rows from the dataframe
d <- d |>
drop_na() |>
mutate(character_BMI = mass / (height/100)^2) |>
group_by(species) |>
mutate(species_mean_BMI = mean(character_BMI))
# 3. adding a column named is_bigger for mean BMI > 30
d <- d |>
mutate(is_bigger = if_else(species_mean_BMI>30, "yes", "no"))
# 4. creating the bar graph
g <- d |>
select(species, species_mean_BMI, is_bigger) |>
distinct() |>
ggplot(aes(x = species, y = species_mean_BMI, fill = is_bigger)) +
geom_bar(stat = "identity")
# 5. adding meaninful labels
g + labs(title = "The biggest species in Star Wars", x = "species",
y = "Body mass index (kg/m^2)", fill = "BMI > 30")
#### 0. Setup Workspace ####
graphics.off()
rm(list=ls())
library(tidyverse)
# 1. loading data and filtering species
# only loading relevant columns
d <- starwars |>
select(name, mass, height, species) |>
filter(str_detect(species, "^(G|D|W)") | str_detect(species, "man$"))
# 2. character BMI, species mean BMI, removing na values
# drop_na() will remove all na rows from the dataframe
d <- d |>
drop_na() |>
mutate(character_BMI = mass / (height/100)^2) |>
group_by(species) |>
mutate(species_mean_BMI = mean(character_BMI))
# 3. adding a column named is_bigger for mean BMI > 30
d <- d |>
mutate(is_bigger = if_else(species_mean_BMI>30, "yes", "no"))
# 4. creating bar graph with appropriate labelling
d |>
select(species, species_mean_BMI, is_bigger) |>
distinct() |>
ggplot(aes(x = species, y = species_mean_BMI, fill = is_bigger)) +
geom_bar(stat = "identity") +
labs(title = "The biggest species in Star Wars", x = "species",
y = "Body mass index (kg/m^2)", fill = "BMI > 30")
